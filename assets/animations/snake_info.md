# Animations Info

To activate the contribution snake animation, setup a GitHub action using `Platane/snk` to generate the SVGs into an `output` branch. The `<picture>` tags are already configured in `stats.md`.
